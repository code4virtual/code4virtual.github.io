# code4virtual.github.io

**Code for Virtual** のウェブサイトです。
バーチャル空間を拠点にしたシビックテック・コミュニティをめざして 2026年8月27日に発足しました。
GitHub Pages（`main` ブランチの `docs/` ディレクトリ）から公開されています。

公開URL: <https://code4virtual.github.io/>

## このサイトの方針

団体は発足したばかりで、活動実績はまだありません。
そのため、このサイトには**確認できる事実だけ**を書いています。

- 決まっていないことは「未定」「準備中」と明示する
- 実績のない活動を、あるかのように書かない
- 決まったことから順に追記していく

内容を追加する際も、この方針を守ってください。

## 構成

```
docs/
├── index.html          トップページ（発足のお知らせ / めざすこと / いまの状況 / これから決めること / 連絡先）
├── 404.html            Not Found ページ
├── robots.txt
├── sitemap.xml
├── .nojekyll           Jekyll のビルドを無効化して静的配信する
└── assets/
    ├── style.css       スタイル（デザイントークンは :root にまとめています）
    ├── main.js         ナビゲーション開閉・スクロール表示（依存ライブラリなし）
    └── favicon.svg
```

ビルド不要の静的サイトです。フレームワークも外部CDNも使っていないため、
`docs/` をローカルで開くだけで確認できます。

```bash
# ローカルで確認する
python3 -m http.server 8000 --directory docs
# → http://localhost:8000
```

## 公開設定（GitHub Pages）

リポジトリの **Settings → Pages** で以下を設定しています。

- Source: `Deploy from a branch`
- Branch: `main` / `/docs`

## 決まったら追記する項目

サイト内の「これから決めること」に対応します。決まり次第、該当箇所を書き換えてください。

| 項目 | 現在の記載 |
| --- | --- |
| 活動テーマ | 未定 |
| 拠点にするプラットフォーム | 未定（VRChat、cluster などを検討） |
| 集まり方・頻度 | 未定 |
| 参加の窓口 | 準備中（現在の連絡手段は GitHub Issues のみ） |
| 成果物の扱い・ライセンス | 未定 |
| OGP 画像 | 未設定 |

## デザイン方針

- ダークテーマ固定。背景のグリッドとグラデーションで雰囲気を出しています
- 色・角丸・余白は `docs/assets/style.css` の `:root` にトークンとして集約
- レスポンシブ対応（900px 以下でナビゲーションをメニュー化）
- スキップリンク、`aria-*` 属性、キーボード操作、`prefers-reduced-motion` に対応

## ライセンス

未定です。方針が決まり次第、追記します。
